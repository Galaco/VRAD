package computerad

import "github.com/galaco/vrad/cmd"

func Main(args *cmd.Args, transfered interface{}) (interface{}, error) {
	if !args.OnlyDetail && !args.OnlyStaticProps {
		//RadWorld_Go()
	}

	return nil,nil
}